version: 2

models:
  - name: dim_customer
    columns:
      - name: sk_customer
        description: Primary key of the customer
        tests:
          - unique
          - not_null
          